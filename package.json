{
  "name": "playground-rust",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "check": "cargo check",
    "format": "cargo fmt --all && git add .",
    "precommit": "npm-run-all -p check format"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "npm-run-all": "^4.1.2"
  }
}
