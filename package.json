{
  "name": "playground-rust",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "test:all": "loop 'cargo fmt --all && cargo clippy --all-targets --all-features -- -D warnings && cargo check && cargo test'"
  },
  "devDependencies": {
    "@commitlint/cli": "^7.2.0",
    "@commitlint/config-conventional": "^7.1.2",
    "husky": "^1.1.1",
    "loop": "^3.0.3"
  }
}
