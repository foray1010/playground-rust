{
  "name": "playground-rust",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "precommit": "loop 'cargo fmt --all && cargo check && cargo test' && git add ."
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "loop": "^3.0.3",
    "npm-run-all": "^4.1.2"
  }
}
