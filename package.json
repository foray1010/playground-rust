{
  "name": "playground-rust",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "precommit": "loop 'cargo check && cargo fmt --all' && git add ."
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "loop": "^3.0.3",
    "npm-run-all": "^4.1.2"
  }
}
